language: node_js
node_js:
  - "node"

branches:
  only:
    - master

env:
  global:
    - NODE_ENV=production

cache: npm

before_deploy:
  - npm run build && npm run package

deploy:
  provider: releases
  api_key: "GITHUB OAUTH TOKEN"
  file: "build/latest_build.zip"
  skip_cleanup: true
